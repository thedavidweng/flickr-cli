version: 2

builds:
  - main: ./cmd/flickr
    binary: flickr
    ldflags:
      - -s -w
      - -X github.com/thedavidweng/flickr-cli/internal/cli.Version={{.Version}}
      - -X github.com/thedavidweng/flickr-cli/internal/cli.Commit={{.ShortCommit}}
      - -X github.com/thedavidweng/flickr-cli/internal/cli.Date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    mod_timestamp: "{{ .CommitTimestamp }}"

archives:
  - formats: [tar.gz]
    name_template: "flickr_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  use: github
  groups:
    - title: "🚀 Features"
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: "🐛 Bug Fixes"
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: "📚 Documentation"
      regexp: '^.*?docs(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: "🧹 Maintenance"
      regexp: '^.*?(chore|refactor|style|test|ci)(\([[:word:]]+\))??!?:.+$'
      order: 3
    - title: "⬆️ Dependencies"
      regexp: '^.*?(build|deps)(\([[:word:]]+\))??!?:.+$'
      order: 4
    - title: "Other"
      order: 999
  filters:
    exclude:
      - "^test:"
      - "^chore: release"
      - "^ci:"
      - "Merge pull request"
      - "Merge branch"

release:
  github:
    owner: thedavidweng
    name: flickr-cli
  header: |
    ## What's Changed

    See the [changelog](https://github.com/thedavidweng/flickr-cli/blob/main/CHANGELOG.md) for full details.
  footer: |
    **Full Changelog**: https://github.com/thedavidweng/flickr-cli/compare/{{ .PreviousTag }}...{{ .Tag }}

    ---
    ### Installation

    **Homebrew (macOS/Linux):**
    ```
    brew tap thedavidweng/tap
    brew install flickr
    ```

    **Scoop (Windows):**
    ```
    scoop bucket add thedavidweng https://github.com/thedavidweng/scoop-bucket
    scoop install flickr
    ```

    **Go:**
    ```
    go install github.com/thedavidweng/flickr-cli/cmd/flickr@{{ .Tag }}
    ```

    **Verify checksum:**
    ```bash
    cosign verify-blob \
      --bundle checksums.txt.sig \
      --certificate-identity-regexp='https://github.com/thedavidweng/flickr-cli' \
      --certificate-oidc-issuer='https://token.actions.githubusercontent.com' \
      checksums.txt
    ```

signs:
  - artifacts: checksum
    cmd: cosign
    args:
      - sign-blob
      - "--yes"
      - "--output-signature=${signature}"
      - "--output-certificate=${certificate}"
      - "${artifact}"

sboms:
  - artifacts: archive
    documents:
      - "${artifact}.sbom.json"

brews:
  - name: flickr
    repository:
      owner: thedavidweng
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/thedavidweng/flickr-cli"
    description: "Agent-friendly CLI for Flickr photo management, backup, upload, and API access"
    license: "MIT"
    install: |
      bin.install "flickr"
      generate_completions_from_executable(bin/"flickr", "completion")
    test: |
      system "#{bin}/flickr", "version"

snapshot:
  version_template: "{{ incpatch .Version }}-next"
